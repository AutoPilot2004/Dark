#version 330 core

in vec4 color;
in vec2 uv;
in float texIndex;

out vec4 outColor;

uniform sampler2D tex[32];

void main()
{
	int index = int(texIndex);

	vec4 texColor = texture(tex[index], uv);

	outColor = texColor * color;
}